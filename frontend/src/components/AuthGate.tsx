import { useState, type FormEvent } from 'react'
import { ThemeToggle } from './ThemeToggle'
export function AuthGate({onLogin}:{onLogin:(token:string)=>void}){const[token,setToken]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();if(token.trim())onLogin(token.trim())};return <main className="auth-shell"><div className="auth-toolbar"><ThemeToggle/></div><section className="auth-card"><div className="brand-mark">M</div><p className="eyebrow">WECHAT OPERATIONS</p><h1>让群聊自动化保持<br/>清晰、可控、可追踪。</h1><p className="muted">输入部署时配置的管理员令牌，进入机器人控制台。</p><form onSubmit={submit}><input className="visually-hidden" aria-hidden="true" tabIndex={-1} autoComplete="username" value="admin" readOnly/><label>管理员令牌<input aria-label="管理员令牌" type="password" value={token} onChange={e=>setToken(e.target.value)} placeholder="Bearer token" autoComplete="current-password" autoFocus/></label><button className="primary" type="submit">进入控制台 <span>↗</span></button></form></section></main>}
