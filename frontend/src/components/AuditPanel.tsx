import type { AuditEvent } from '../api/types'
export function AuditPanel({events}:{events:AuditEvent[]}){return <section className="panel audit-panel"><div className="panel-heading"><div><p className="eyebrow">AUDIT TRAIL</p><h2>操作审计</h2></div></div><div className="audit-list">{events.length===0?<div className="empty-state">关键操作完成后会生成审计记录。</div>:events.slice(0,12).map(event=><div className="audit-row" key={event.id}><span className={`audit-result ${event.result}`}>{event.result==='success'?'✓':'!'}</span><div><b>{event.action}</b><small>{event.detail??event.targetId}</small></div><time>{new Date(event.createdAt).toLocaleString()}</time></div>)}</div></section>}
