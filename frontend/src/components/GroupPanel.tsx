import type { Group } from '../api/types'
export function GroupPanel({groups,selectedId,onSelect,onToggle}:{groups:Group[];selectedId:string;onSelect:(id:string)=>void;onToggle:(group:Group)=>void}){return <section className="panel"><div className="panel-heading"><div><p className="eyebrow">AUTHORIZED ROOMS</p><h2>群聊范围</h2></div><span className="count">{groups.filter(g=>g.enabled).length}/{groups.length}</span></div><div className="group-list">{groups.length===0?<div className="empty-state">机器人登录后，群聊会显示在这里。</div>:groups.map(group=><button key={group.id} className={`group-row ${selectedId===group.id?'selected':''}`} onClick={()=>onSelect(group.id)}><span className="avatar">{group.name.slice(0,1)}</span><span className="group-main"><b>{group.name}</b><small>{group.memberCount??'—'} 位成员 · {group.available?'可用':'不可用'}</small></span><span className="toggle-wrap" onClick={e=>e.stopPropagation()}><input aria-label={`启用 ${group.name}`} type="checkbox" checked={group.enabled} onChange={()=>onToggle(group)}/><i/></span></button>)}</div></section>}
