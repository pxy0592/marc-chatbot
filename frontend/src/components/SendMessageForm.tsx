import { useState, type FormEvent } from 'react'
import type { Group } from '../api/types'
export function SendMessageForm({group,onSend,busy}:{group?:Group;onSend:(content:string)=>Promise<void>;busy:boolean}){const[text,setText]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();if(!text.trim())return;await onSend(text.trim());setText('')};return <form className="composer" onSubmit={submit}><div><p className="eyebrow">SEND TO ROOM</p><h3>{group?.name??'选择一个群聊'}</h3></div><textarea aria-label="消息内容" value={text} onChange={e=>setText(e.target.value)} maxLength={2000} placeholder={group?.enabled?'输入要发送到群里的文本消息…':'启用群聊后可以发送消息'} disabled={!group?.enabled||busy}/><div className="composer-footer"><small>{text.length}/2000</small><button className="primary" disabled={!group?.enabled||!text.trim()||busy}>发送消息 ↗</button></div></form>}
